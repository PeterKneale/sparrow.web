import React from 'react';
import Header from '../../components'

const Users = () => {
    return (
        <div>
            <Header heading="Users"/>
        </div>
    );
};

export default Users;